{
  "name": "catalog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "python3 ../tools/schema-tools.py bundle ../schemas ../dist && mkdir -p public/schemas && cp -r ../dist/domains ../dist/_common public/schemas/ && node scripts/generate-feed.mjs",
    "predev": "python3 ../tools/schema-tools.py bundle ../schemas ../dist && mkdir -p public/schemas && cp -r ../dist/domains ../dist/_common public/schemas/ && node scripts/generate-feed.mjs",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1",
    "fuse.js": "^7.1.0",
    "next": "16.1.7",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-simple-code-editor": "^0.14.1",
    "shiki": "^4.0.2"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.7",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
